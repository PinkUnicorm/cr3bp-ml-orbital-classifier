# cr3bp-ml-orbital-classifier
This project uses machine learning to predict the behavior of a small object in a three-body gravitational system. Given initial conditions, the model classifies whether the object will remain in orbit, escape, or collide, while a physics simulation visualizes the trajectory. 
